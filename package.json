{
  "name": "my_project",
  "version": "0.0.1",
  "description": "it does some things (?)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^12.0.1",
    "grunt": "^0.4.5",
    "watch": "^0.16.0"
  },
  "dependencies": {
    "derivable": "^0.9.3"
  }
}
